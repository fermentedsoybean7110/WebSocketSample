const WebSocketServer = require('ws').Server;

// ポート番号を指定してください
const wss = new WebSocketServer({ port:XXXX });

wss.on('connection', ws_client => {

  ws_client.send('server is connected');

  ws_client.on('message', data => {
    console.log('送られてきたデータ: %s', data);
  });
});